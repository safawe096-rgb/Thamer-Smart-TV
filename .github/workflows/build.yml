name: Build Thamer TV APK
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libssl-dev cmake

      - name: Install Buildozer and Kivy
        run: pip install buildozer cython kivy

      - name: Create Custom Buildozer Spec and Build
        run: |
          buildozer init
          sed -i 's/# requirements = kivy/requirements = python3,kivy/g' buildozer.spec
          sed -i 's/package.name = myapp/package.name = thamertv/g' buildozer.spec
          sed -i 's/title = My Application/title = THAMER TV/g' buildozer.spec
          yes | buildozer -v android debug

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: ThamerTV-StandAlone-APK
          path: bin/*.apk
